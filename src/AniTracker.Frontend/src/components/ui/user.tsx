import { api } from "@/utils/Api";
import { ReactNode } from "react";

export default async function User(): Promise<ReactNode> {
    // const user = await api.get("/users/me");

    return <div></div>;
}
