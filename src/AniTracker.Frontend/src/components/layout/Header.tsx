import type { ReactNode } from "react";

export default function Header(): ReactNode {
    return (
        <header>
            <div>
                <></>
            </div>
        </header>
    )
}